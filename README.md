# VerseOfTheDayCLITool

A fun low level project that grabs a verse of the day and displays it in the terminal. Using C code.

# Things-to-do/consider

Command to build and run: `curl-config --cc --cflags` -o main main.c `curl-config --libs`
